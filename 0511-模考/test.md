4
1 0
2 1
1 0
2
3
3

---

初始状态           1
2号插在 1 的左边    2 1
3号插在 2 的右边    2 3 1
4号插在 1 的左边    2 3 4 1

删除 3 号          2 4 1
删除 3 号          2 4 1


// 插入
a[k].pre -> k -> a[k].nxt
a[k].pre -> k -> i -> a[k].nxt

// 删除
a[k].pre -> k -> a[k].nxt
a[k].pre -> a[k].nxt